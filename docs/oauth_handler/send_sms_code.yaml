发送验证码
---
tags:
  - 授权认证
summary: 发送验证码
description:
operationId: send_sms_code
requestBody:
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/SendSMSCodeReq"
responses:
  x-200:
    $ref: "#/components/responses/BaseResponse"
  "200":
    content:
      application/json:
        schema:
          required:
            - "code"
            - "data"
            - "message"
          properties:
            code:
              type: "string"
              example: "success"
            data:
              type: "object"
              example: {}
            message:
              type: "string"
              example: "短信验证码发送成功"
