更新API Key启用状态
---
tags:
  - 开发API管理
summary: 更新API Key启用状态
description: 更新API Key启用状态
operationId: update_api_key_is_active
parameters:
  - name: api_key_id
    in: path
    required: true
    schema:
      type: string
      default: "a72fb5a8-e8b5-4141-9a56-2f8b17407371"
    format: uuid
requestBody:
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/UpdateApiKeyIsActiveReq"
responses:
  "200":
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/BaseRespBody"
        example:
          code: "success"
          message: "更新API Key启用状态成功"
          data: {}
