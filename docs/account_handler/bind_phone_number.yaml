绑定手机号
---
tags:
  - 账号管理
summary: 绑定手机号
description:
operationId: bind_phone_number
requestBody:
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/BindPhoneNumberReq"
responses:
  x-200:
    $ref: "#/components/responses/BaseResponse"
  "200":
    content:
      application/json:
        schema:
          required:
            - "code"
            - "data"
            - "message"
          properties:
            code:
              type: "string"
              example: "success"
            data:
              type: "object"
              example: {}
            message:
              type: "string"
              example: "绑定手机号成功"
