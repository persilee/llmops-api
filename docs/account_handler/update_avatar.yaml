修改用户头像
---
tags:
  - 账号管理
summary: 修改用户头像
description:
operationId: update_avatar
requestBody:
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/UpdateAvatarReq"
responses:
  x-200:
    $ref: "#/components/responses/BaseResponse"
  "200":
    content:
      application/json:
        schema:
          required:
            - "code"
            - "data"
            - "message"
          properties:
            code:
              type: "string"
              example: "success"
            data:
              type: "object"
              example: {}
            message:
              type: "string"
              example: "修改头像成功"
