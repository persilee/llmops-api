绑定第三方登录
---
tags:
  - 账号管理
summary: 绑定第三方登录
description:
operationId: unbind_oauth_provider
requestBody:
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/UnbindOAuthProviderReq"
responses:
  x-200:
    $ref: "#/components/responses/BaseResponse"
  "200":
    content:
      application/json:
        schema:
          required:
            - "code"
            - "data"
            - "message"
          properties:
            code:
              type: "string"
              example: "success"
            data:
              type: "object"
              example: {}
            message:
              type: "string"
              example: "绑定成功"
