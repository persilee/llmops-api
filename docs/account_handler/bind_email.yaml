绑定邮箱
---
tags:
  - 账号管理
summary: 绑定邮箱
description:
operationId: bind_email
requestBody:
  content:
    application/json:
      schema:
        $ref: "#/components/schemas/BindEmailReq"
responses:
  x-200:
    $ref: "#/components/responses/BaseResponse"
  "200":
    content:
      application/json:
        schema:
          required:
            - "code"
            - "data"
            - "message"
          properties:
            code:
              type: "string"
              example: "success"
            data:
              type: "object"
              example: {}
            message:
              type: "string"
              example: "绑定邮箱成功"
