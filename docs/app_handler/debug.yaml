机器人调天接口
---
tags:
  - App管理
summary: 机器人调天接口
description: >
  用于调试模型参数的接口，可以通过该接口获取模型参数的调试信息。
operationId: debug
parameters:
  - $ref: "#/components/parameters/appIdParam"
requestBody:
  description: 聊天内容
  required: true
  content:
    application/json:
      schema:
        type: object
        required:
          - query
        properties:
          query:
            type: string
            description: 聊天内容
            example: "你好"
responses:
  "200":
    content:
      application/json:
        schema:
          allOf:
            - $ref: "#/components/schemas/BaseRespBody"
            - type: object
              properties:
                data:
                  type: object
                  properties:
                    content:
                      type: string
        example:
          code: "success"
          message: ""
          data:
            content: "你好！我是一个人工智能助手，可以回答你的问题，帮助你获取信息和完成各种任务。如果你有任何问题或需要帮助，可以随时告诉我！"
